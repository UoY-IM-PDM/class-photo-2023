function setup() {
    createCanvas(1000, 600);
}

function draw() {
    background(255);
}

/**
 * INSTRUCTIONS - DO NOT DELETE
 * Add your code for creating your character below (either a class or function). To avoid conflicting names, 
 * include your GitHub username (or your uni username) in the function or class name.
 * 
 * Finally, call your function / class in draw to add your character to the photo
 */